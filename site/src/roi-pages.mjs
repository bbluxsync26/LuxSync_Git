import guides from './roi/catalog.json' with {type:'json'};
export {guides};
const e=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const rich=s=>e(s).replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/`([^`]+)`/g,'$1');
export const cover=g=>`<div class="roi-cover"><img src="/assets/roi/cover-master.png" alt="The ROI of Smart Living: ${e(g.title)}" width="1024" height="1448" loading="lazy"><span class="roi-cover-subtitle" aria-hidden="true">${g.coverLines.map(e).join('<br>')}</span></div>`;
export function guideLibrary(){return `<section class="page-hero"><div><p class="eyebrow">LuxSync ROI Guide Library</p><h1>Measure what matters to you.</h1><p>Choose your guide. Download the PDF, or keep an online worksheet with usage logs and comparisons in your account.</p><p>Record your own values now. Automatic ROI calculations will follow later.</p></div></section><section class="section"><div class="guide-groups">${[...new Set(guides.map(g=>g.group))].map(group=>`<section class="guide-group"><h2>${e(group)}</h2><div class="roi-library-grid">${guides.filter(g=>g.group===group).map(g=>`<article class="roi-library-card">${cover(g)}<div><h3>${e(g.title)}</h3><p>${e(g.audience)}</p><div class="button-row"><a class="button" href="/guides/${g.id}/">Use online guide</a><a class="text-link" href="/downloads/roi/${g.id}.pdf" download>Download PDF ↓</a></div></div></article>`).join('')}</div></section>`).join('')}</div></section>`;}
function block(b){
  if(b.type==='heading')return `<h3>${rich(b.text)}</h3>`;
  if(b.type==='paragraph')return `<p>${rich(b.text)}</p>`;
  if(b.type==='list')return `<ul>${b.items.map(s=>`<li>${rich(s)}</li>`).join('')}</ul>`;
  if(b.type==='formula')return `<p class="field-hint">Reference method only. Calculations are not automated in this edition.</p><pre>${e(b.text)}</pre>`;
  if(b.type==='table')return `<div class="table-scroll" tabindex="0" role="region" aria-label="Guide reference table"><table><thead><tr>${b.rows[0].map(s=>`<th scope="col">${rich(s)}</th>`).join('')}</tr></thead><tbody>${b.rows.slice(1).map(r=>`<tr>${r.map(s=>`<td>${rich(s)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
  return '';
}
export function guidePage(g){return `<section class="section roi-guide-intro">${cover(g)}<div><p class="eyebrow">The ROI of Smart Living</p><h1>${e(g.title)}</h1><p>${e(g.audience)}</p><p>${e(g.intro)}</p><div class="button-row"><a class="button button-secondary" href="/downloads/roi/${g.id}.pdf" download>Download PDF</a><a class="text-link" href="/guides/">All guides →</a></div></div></section><section class="section roi-workspace" data-roi-guide="${g.id}"><div class="workspace-toolbar"><nav aria-label="Guide sections"><a href="#guide-reading">Read guide</a><a href="#guide-worksheet">Worksheet</a><a href="#guide-log">Usage log</a><a href="#guide-compare">Compare</a></nav><button class="button" type="button" data-save-guide>Save guide to my account</button></div><div data-account-notice class="account-notice"></div><p class="save-status" data-guide-status role="status"></p><details class="guide-reading" id="guide-reading"><summary>Read the complete ${e(g.title)} guide</summary><article class="guide-prose">${g.blocks.map(block).join('')}</article></details><form id="guide-worksheet" class="roi-worksheet"><p class="eyebrow">Your online worksheet</p><h2>Start with your own baseline.</h2><p>Use the reference methods to plan your measurements. No savings, ROI, or payback is calculated automatically.</p><label>Worksheet / property name<input name="projectName" maxlength="120" placeholder="For example, Lakeside property" required></label><label>Goals and context<textarea name="notes" rows="3" maxlength="6000" placeholder="What would you like to learn or improve?"></textarea></label><div class="worksheet-fields">${g.worksheet.map(f=>`<label>${e(f.label)}<input name="${f.id}" maxlength="300" placeholder="${e(f.unit)}"><span class="field-hint">${e(f.method)}</span></label>`).join('')}</div></form><section id="guide-log" class="roi-log"><p class="eyebrow">Usage log</p><h2>Record a measurement period.</h2><p>Keep the same units across periods. Record source information and context alongside the values.</p><form id="measurement-form"><div class="field-row"><label>Period label<input name="label" required maxlength="160" placeholder="January baseline"></label><label>Recorded date<input name="date" type="date" required></label><label>Phase<select name="phase"><option>Baseline</option><option>Pilot</option><option>Follow-up</option></select></label></div><div class="measurement-fields">${g.metrics.map(m=>`<fieldset><legend>${e(m.label)}</legend><p class="field-hint">${e(m.hint)}</p><div class="field-row"><label>Value<input name="${m.id}-value" maxlength="250" placeholder="Recorded value"></label><label>Unit / measure<input name="${m.id}-unit" maxlength="80" placeholder="kWh, hours, incidents…"></label></div></fieldset>`).join('')}</div><label>Evidence and context<textarea name="notes" maxlength="6000" rows="3" placeholder="Source, occupancy, weather, equipment changes, or estimates"></textarea></label><button class="button button-secondary" type="submit">Add measurement period</button></form><div data-log-list></div></section><section id="guide-compare" class="roi-compare"><p class="eyebrow">Compare your records</p><h2>Two periods, side by side.</h2><p>Values are shown as entered. There are no calculated differences or projected savings.</p><div class="field-row"><label>Period A<select data-compare-a></select></label><label>Period B<select data-compare-b></select></label></div><div class="table-scroll" data-comparison></div></section><div class="button-row"><button class="button" type="button" data-save-guide>Save guide to my account</button><a class="text-link" href="/account/welcome/">My saved guides →</a></div></section>`;}

export function accountPage(create=false){return `<section class="auth-page"><div class="auth-visual"><img src="/assets/member-access.png" alt=""><div><img src="/assets/luxsync-horizontal-combo.png" alt="LuxSync"><p class="eyebrow">Your LuxSync workspace</p><h1>${create?'Create your account':'Welcome back'}</h1><p>Keep your ROI guides, measurement history, and Concierge choices together.</p></div></div><div class="auth-panel"><section class="auth-card account-entry"><h2>${create?'Start your LuxSync workspace':'Sign in to LuxSync'}</h2><p>Use ChatGPT sign-in to access your saved LuxSync work. Your workspace is created on your first sign-in.</p><div data-account-notice class="account-notice"></div><a class="button" data-account-signin href="/signin-with-chatgpt?return_to=%2Faccount%2Fwelcome%2F" target="_top">${create?'Create account with ChatGPT':'Sign in with ChatGPT'}</a><a href="${create?'/account/':'/account/create/'}" data-account-switch>${create?'Already have an account? Sign in':'New here? Create an account'}</a><a href="/guides/">Browse the free PDF guides</a></section></div></section>`;}
export function accountDashboard(){return `<section class="welcome-hero"><img src="/assets/member-access.png" alt=""><div><p class="eyebrow">Your LuxSync workspace</p><h1>Keep building on what you learn.</h1><p>Your saved ROI guides, recorded periods, and Concierge choices.</p></div></section><section class="section"><div data-account-notice class="account-notice"></div><div class="button-row"><a class="button" href="/guides/">Start an ROI guide</a><a class="button button-secondary" href="/find-my-luxsync-solution/">Explore with Concierge</a><a class="text-link" href="/signout-with-chatgpt?return_to=%2Faccount%2F" data-account-signout target="_top">Sign out</a></div><div id="saved-work" aria-live="polite"><p>Loading your saved work…</p></div></section>`;}
export const savePrompt=`<dialog id="account-save-prompt" class="search-dialog" aria-labelledby="save-prompt-title"><div class="search-dialog-top"><h2 id="save-prompt-title">Keep this in your account</h2><button type="button" class="button" data-close-save>Close</button></div><p>Sign in or create an account to save your work and return to it later. Your current draft will stay in this tab during sign-in.</p><div data-account-notice class="account-notice"></div><div class="button-row"><a class="button" data-prompt-signin href="/account/">Sign in</a><a class="button button-secondary" data-prompt-create href="/account/create/">Create an account</a></div></dialog>`;
export const conciergeSave=`<section class="section concierge-save"><div><p class="eyebrow">Make room for later</p><h2>Keep your choices in your account.</h2><p>Save this Concierge journey or Blueprint and pick it up when you’re ready.</p><div data-account-notice class="account-notice"></div></div><div><button type="button" class="button" data-save-concierge>Save my Concierge choices</button><p data-concierge-save-status role="status"></p><a class="text-link" href="/account/welcome/">My saved work →</a></div></section>`;
